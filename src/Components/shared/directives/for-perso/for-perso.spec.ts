import { ForPerso } from './for-perso';

describe('ForPerso', () => {
  it('should create an instance', () => {
    const directive = new ForPerso();
    expect(directive).toBeTruthy();
  });
});
