import { TofahrenheitPipe } from './tofahrenheit-pipe';

describe('TofahrenheitPipe', () => {
  it('create an instance', () => {
    const pipe = new TofahrenheitPipe();
    expect(pipe).toBeTruthy();
  });
});
