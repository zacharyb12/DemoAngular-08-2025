import { EmojiFormatPipe } from './emoji-format-pipe';

describe('EmojiFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new EmojiFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
